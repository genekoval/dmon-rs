[package]
name = "dmon"
version = "0.1.1"
edition = "2024"

[dependencies]
nix = { version = "0.30", features = ["fs", "process", "user"] }

[dev-dependencies]
clap = { version = "4", features = ["derive", "string", "wrap_help"] }
